

add beneficiaries as a list to the account object. Make it possbile to edit this list separately.

Now lets revisit the idempotency of the file uploads. Lets restructure the code to allow for duplicates in the same file
but prevent duplicates if the filename is different for a transaction given on the same date, category and amount.
Draft a plan on how this would be implemented, and also capture all the code that needs to be deleted once this change is
made. Additionally weigh on the pros and cons of this approach and identify any use-cases that would break due to this


So lets work on idempotency
